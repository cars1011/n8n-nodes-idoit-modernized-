// This file is intentionally close to empty - n8n discovers nodes and
// credentials via the "n8n" field in package.json, not through this entry
// point. It only needs to exist and be resolvable as `main`.
module.exports = {};
